server:
  port: 8081
spring:
  application:
    name: my-app
  datasource:
    url: jdbc:mariadb://localhost:3306/random1
    username: root
    password: password1
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
management:
  endpoints:
    web:
      exposure:
        include:
        - '*'    