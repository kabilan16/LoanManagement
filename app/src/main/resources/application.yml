server:
  port: 8081
spring:
  application:
    name: my-app
  datasource:
    url: jdbc:mariadb://localhost:3306/random
    username: root
    password: Password@1
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
management:
  endpoints:
    web:
      exposure:
        include:
        - '*'    